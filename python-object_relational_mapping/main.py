import MySQLdb
import sys

def list_states(username, password, )
db = MySQLdb.connect( host="localhost",
                     user="sammy",
                     password="password",
                     db="hbtn_0e_usa")
cursor = db.cursor()